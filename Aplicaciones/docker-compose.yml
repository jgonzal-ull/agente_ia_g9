services:
  app:
    image: agente_ia_g9
    container_name: agente_ia_g9
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - d:\BOB\Agente IA G9\:/app
    working_dir: /app
    stdin_open: true
    tty: true
    entrypoint: >
      sh -c "/bin/bash"